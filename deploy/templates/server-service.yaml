apiVersion: v1
kind: Service
metadata:
  name: mern-server
  namespace: mern-app
spec:
  selector:
    app: mern-server
  ports:
  - port: 5000
    targetPort: {{ .Values.server.port }}
  type: ClusterIP